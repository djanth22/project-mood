export default async function Search() {
  // const SearchText = formData.get("search-text");
  // const search = await db.query("SELECT * FROM moods WHERE name = $1;",[SearchText])
  return (
    <>
      <div>
        {/* <form>
          <input type="text" id="search-text" name="search-text" />
        </form> */}
        <p>search</p>
      </div>
      <div>
        <p>search results</p>
      </div>
    </>
  );
}
